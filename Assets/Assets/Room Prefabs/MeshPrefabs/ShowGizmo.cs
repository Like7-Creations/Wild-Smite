using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class ShowGizmo : MonoBehaviour
{
    //private void OnDrawGizmos()
    //{
    //    Gizmos.color = Color.yellow;
    //    Gizmos.DrawCube(transform.position, Vector3.one);

    //    GUIStyle style = new GUIStyle();
    //    style.normal.textColor = Color.white;
    //    style.fontSize = 8;
    //    style.alignment = TextAnchor.MiddleCenter;

    //    Vector3 pos = transform.position;
    //    pos.y += 5;

    //    Vector3 camPos = SceneView.lastActiveSceneView.camera.transform.position;
    //    Quaternion rot = Quaternion.LookRotation(pos - camPos, Vector3.up);

    //    Handles.Label(pos, transform.name, style);
    //    Handles.matrix = Matrix4x4.TRS(pos, rot, Vector3.one);
    //}
}
